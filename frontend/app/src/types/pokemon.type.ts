////////////////////////////////////////////////////////////////////////////////
/** A type that distills down the information we get from the `pokemon` endpoint. */
export interface BasicPokemonInfo {
    id: number,
    name: string,
};
